const arrChunk = (arr, chunkSize) => {
//   let arrOfArr = [];
//   for (let i = 0; i < arr.length - 1; i+chunkSize) {
//     const chunk = arr.slice(i, i + chunkSize);
//     arrOfArr.push(chunk);
//   }
//   return arrOfArr;
// };

// console.log(arrChunk(arr,3))